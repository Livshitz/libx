style.
	@media screen and (max-width: 599px){ 	}

.inlineController.content-page(ng-controller="inlineController", layout="column", layout-align="start start").padding
	
	h1 Not Found!
	
	
	a(ng-click="infra.browser.angular.goBack()") Back

script.
	infra.browser.angular.lazy.controller('inlineController', function ($scope, $rootScope, $controller, $timeout, $interval, $routeParams) {
		angular.extend(this, $controller('inlineControllerBase', {$scope: $scope}));
		
	});
